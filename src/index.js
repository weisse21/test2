import React from "react"
import Dom from "react-dom"
import Navbar from "./components/navbar";

const root = Dom.createRoot(document.getElementById("root"));
const element = <h1>Navbar</h1>;
root.render(element)

